import React from "react";
import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";

const ErrorAnalysis = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4">Error Analysis Page</Typography>
    </Box>
  );
};

export default ErrorAnalysis;
